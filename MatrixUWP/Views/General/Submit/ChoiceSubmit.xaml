<Page
    x:Class="MatrixUWP.Views.General.Submit.ChoiceSubmit"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MatrixUWP.Views.General.Submit"
    xmlns:app="using:MatrixUWP.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:choice="using:MatrixUWP.Models.Course.Assignment.Choice"
    xmlns:controls1="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:controls2="using:MatrixUWP.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowMediumHighBrush}" Loaded="Page_Loaded">

    <Grid Margin="8,0">
        <TextBlock x:Name="TitleViewer" Margin="8" FontSize="22" Text="{x:Bind viewModel.Title, Mode=OneWay}"></TextBlock>
        <Button x:Name="Submit" Margin="0,8,8,0" Content="提交" VerticalAlignment="Top" HorizontalAlignment="Right" Click="Submit_Click"></Button>
        <Grid Margin="8,54,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <ScrollViewer Margin="0,0,16,0" Grid.Column="0" x:Name="DescriptionViewer">
                <controls1:MarkdownTextBlock
                                LinkClicked="MarkdownTextBlock_LinkClicked"
                                ImageClicked="MarkdownTextBlock_ImageClicked"
                                Background="Transparent"
                                UriPrefix="{x:Bind app:AppModel.MatrixHttpClient.BaseUri.AbsoluteUri}"
                                Text="{x:Bind viewModel.Description, Mode=OneWay}"></controls1:MarkdownTextBlock>
            </ScrollViewer>
            <controls1:GridSplitter
                GripperCursor="Default"
                HorizontalAlignment="Left"
                ResizeDirection="Auto"
                Grid.Column="1"
                ResizeBehavior="BasedOnAlignment"
                CursorBehavior="ChangeOnSplitterHover"
                Width="16">
            </controls1:GridSplitter>
            <ScrollViewer x:Name="QuestionContainer" Margin="16,0,0,0" Grid.Column="1">
                <controls:ItemsRepeater ItemsSource="{x:Bind viewModel.Questions, Mode=OneWay}">
                    <controls:ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="choice:ChoiceAssignmentQuestion">
                            <StackPanel Margin="8,8,0,0">
                                <controls1:WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <TextBlock Margin="0,0,0,0" Text="{x:Bind Id, Converter={StaticResource IndexOffsetConverter}}"></TextBlock>
                                    <TextBlock Margin="0,0,8,0" Text="."></TextBlock>
                                    <StackPanel Orientation="Vertical">
                                        <controls1:MarkdownTextBlock
                                            LinkClicked="MarkdownTextBlock_LinkClicked"
                                            ImageClicked="MarkdownTextBlock_ImageClicked"
                                            Background="Transparent"
                                            UriPrefix="{x:Bind app:AppModel.MatrixHttpClient.BaseUri.AbsoluteUri}"
                                            Text="{x:Bind Description}"></controls1:MarkdownTextBlock>
                                        <TextBlock Foreground="Gray" Text="{x:Bind Explanation}"></TextBlock>
                                    </StackPanel>
                                </controls1:WrapPanel>
                                <StackPanel>
                                    <controls:ItemsRepeater x:Name="MultipleChoicePanel" x:Load="{x:Bind IsMultipleChoice}" Margin="20,0,0,0" ItemsSource="{x:Bind Choices}">
                                        <controls:ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="choice:ChoiceAssignmentElement">
                                                <CheckBox IsThreeState="False" IsChecked="{x:Bind IsChecked, Mode=TwoWay}">
                                                    <controls1:WrapPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                                                        <TextBlock Text="{x:Bind Id, Converter={StaticResource NumberLetterConverter}}"></TextBlock>
                                                        <TextBlock Text="."></TextBlock>
                                                        <controls1:MarkdownTextBlock Margin="8,0,0,0"
                                                            LinkClicked="MarkdownTextBlock_LinkClicked"
                                                            ImageClicked="MarkdownTextBlock_ImageClicked"
                                                            Background="Transparent"
                                                            UriPrefix="{x:Bind app:AppModel.MatrixHttpClient.BaseUri.AbsoluteUri}"
                                                            Text="{x:Bind Description}"></controls1:MarkdownTextBlock>
                                                    </controls1:WrapPanel>
                                                </CheckBox>
                                            </DataTemplate>
                                        </controls:ItemsRepeater.ItemTemplate>
                                    </controls:ItemsRepeater>
                                    <controls:ItemsRepeater x:Name="SingleChoicePanel" x:Load="{x:Bind IsMultipleChoice, Converter={StaticResource NotConverter}}" Margin="20,0,0,0" ItemsSource="{x:Bind Choices}">
                                        <controls:ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="choice:ChoiceAssignmentElement">
                                                <RadioButton GroupName="{x:Bind Question.Id}" IsChecked="{x:Bind IsChecked, Mode=TwoWay}">
                                                    <controls1:WrapPanel Orientation="Horizontal" VerticalAlignment="Bottom">
                                                        <TextBlock Text="{x:Bind Id, Converter={StaticResource NumberLetterConverter}}"></TextBlock>
                                                        <TextBlock Text="."></TextBlock>
                                                        <controls1:MarkdownTextBlock Margin="8,0,0,0"
                                                            LinkClicked="MarkdownTextBlock_LinkClicked"
                                                            ImageClicked="MarkdownTextBlock_ImageClicked"
                                                            Background="Transparent"
                                                            UriPrefix="{x:Bind app:AppModel.MatrixHttpClient.BaseUri.AbsoluteUri}"
                                                            Text="{x:Bind Description}"></controls1:MarkdownTextBlock>
                                                    </controls1:WrapPanel>
                                                </RadioButton>
                                            </DataTemplate>
                                        </controls:ItemsRepeater.ItemTemplate>
                                    </controls:ItemsRepeater>
                                </StackPanel>
                                <Grid>
                                    <TextBlock Margin="20,0,0,0" Foreground="Gray" Text="{x:Bind StandardAnswerDisplayText}"></TextBlock>
                                    <Button Content="重置" HorizontalAlignment="Right" Click="Reset_Click" Tag="{x:Bind ResetSelection, Mode=OneWay}"></Button>
                                </Grid>
                            </StackPanel>
                        </DataTemplate>
                    </controls:ItemsRepeater.ItemTemplate>
                </controls:ItemsRepeater>
            </ScrollViewer>
        </Grid>

        <controls:TeachingTip x:Name="LoadPreviousSubmissionTip"
                              Target="{x:Bind QuestionContainer}"
                              Title="是否加载上一次的提交内容？"
                              Subtitle="注意：加载后将覆盖当前答题状态"
                              ActionButtonClick="LoadSubmission_Clicked"
                              ActionButtonContent="是"
                              CloseButtonContent="否">
            <controls:TeachingTip.IconSource>
                <controls:SymbolIconSource Symbol="Refresh" />
            </controls:TeachingTip.IconSource>
        </controls:TeachingTip>
        <ProgressRing HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-50,0,0" Width="60" Height="60" IsActive="{x:Bind viewModel.Loading, Mode=OneWay}"></ProgressRing>
    </Grid>

</Page>
